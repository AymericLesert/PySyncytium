# Test database connection

class: MySQL

parameters:
  hostname: "${PSTEST_DATABASE_HOSTNAME?localhost}"
  username: "${PSTEST_DATABASE_USERNAME}"
  password*: ${PSTEST_DATABASE_PASSWORD}
  schema: "PSTest"